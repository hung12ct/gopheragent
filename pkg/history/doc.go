// Package history defines conversation storage types and session backends.
//
// Backends include in-memory, file-based, and MySQL implementations via a
// unified session manager contract.
package history
